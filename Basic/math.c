#include <math.h>
#include <stdio.h>
#include <stdlib.h>

int main() {
  double square, fab, powe, ascos, assin, astan, astan2, coss, sinn, tann;
  
  square = sqrt(1.0); // 루트
  fab = fabs(-9.0); // abs(-9.0)도 된다. 절댓값
  powe = pow(8.0, 9.0); // 8^9. 제곱
  ascos = acos(1.0); // 아크 코사인
  
  return 0;
}
